/* ── THAIZEN WEB APP — SHARED STYLES ── */
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;1,300;1,400&family=DM+Sans:ital,wght@0,300;0,400;0,500;1,400&display=swap');

:root {
  --gold:     #b8902a;
  --gold-l:   #d4af6a;
  --gold-p:   #f5ecd6;
  --gold-dim: rgba(184,144,42,0.15);
  --dark:     #1a1510;
  --dark2:    #252018;
  --dark3:    #2e2820;
  --stone:    #3d3428;
  --bark:     #7a6248;
  --clay:     #c4a06a;
  --linen:    #fdf8f0;
  --mist:     #ede4d2;
  --parchment:#e8dac0;
  --sage:     #7a8c6a;
  --sage-l:   #afc09a;
  --white:    #ffffff;
  --ok:       #4a7a4a;
  --err:      #b03030;

  --r:     14px;
  --r-sm:  8px;
  --r-lg:  20px;
  --max-w: 480px;

  --shadow-sm: 0 2px 10px rgba(58,52,40,0.08);
  --shadow:    0 4px 24px rgba(58,52,40,0.12);
  --shadow-lg: 0 12px 48px rgba(58,52,40,0.18);
}

* { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }
html { font-size:16px; }
body {
  font-family: 'DM Sans', sans-serif;
  background: var(--linen);
  color: var(--stone);
  min-height: 100vh;
  -webkit-font-smoothing: antialiased;
}

/* ── LAYOUT ── */
.app-shell {
  max-width: var(--max-w);
  margin: 0 auto;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  position: relative;
  background: var(--linen);
  box-shadow: 0 0 80px rgba(0,0,0,0.08);
}

/* ── TOP BAR ── */
.top-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px 20px;
  background: var(--linen);
  border-bottom: 1px solid rgba(0,0,0,0.05);
  position: sticky; top: 0; z-index: 100;
}
.top-bar.dark {
  background: var(--dark);
  border-bottom: 1px solid rgba(255,255,255,0.05);
}
.top-bar-back {
  display: flex; align-items: center; gap: 6px;
  background: none; border: none; cursor: pointer;
  font-family: 'DM Sans', sans-serif;
  font-size: 0.85rem; color: var(--gold);
  padding: 6px 0; min-width: 60px;
}
.top-bar-back svg { width:18px; height:18px; flex-shrink:0; }
.top-bar-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: 1.05rem; font-weight: 500;
  color: var(--stone); letter-spacing: 0.04em;
}
.top-bar.dark .top-bar-title { color: var(--gold-p); }
.top-bar-action {
  min-width: 60px; text-align: right;
  font-size: 0.78rem; color: var(--gold);
  cursor: pointer; background: none; border: none;
  font-family: 'DM Sans', sans-serif;
}

/* ── PROGRESS ── */
.progress-bar {
  padding: 12px 20px 14px;
  background: var(--linen);
  border-bottom: 1px solid rgba(0,0,0,0.04);
}
.progress-track { display: flex; gap: 5px; margin-bottom: 6px; }
.progress-pip {
  flex: 1; height: 3px; border-radius: 2px;
  background: var(--mist); transition: background 0.3s;
}
.progress-pip.done   { background: var(--sage-l); }
.progress-pip.active { background: var(--gold); }
.progress-text {
  font-size: 0.65rem; color: var(--bark);
  letter-spacing: 0.08em; opacity: 0.6;
}

/* ── PAGE BODY ── */
.page-body {
  flex: 1;
  padding: 20px 20px 120px;
  overflow-y: auto;
}
.page-body.dark { background: var(--dark); }

/* ── SECTION LABEL ── */
.section-label {
  font-size: 0.63rem; letter-spacing: 0.16em;
  text-transform: uppercase; color: var(--bark);
  opacity: 0.55; margin-bottom: 10px; margin-top: 20px;
}
.section-label:first-child { margin-top: 0; }

/* ── CARDS ── */
.card {
  background: var(--white);
  border-radius: var(--r);
  padding: 16px;
  box-shadow: var(--shadow-sm);
  margin-bottom: 12px;
}
.card.dark-card {
  background: var(--dark3);
  border: 1px solid rgba(255,255,255,0.05);
  box-shadow: none;
}

/* ── FORM FIELDS ── */
.field { margin-bottom: 12px; }
.field:last-child { margin-bottom: 0; }
.field label {
  display: block; font-size: 0.7rem; font-weight: 500;
  color: var(--bark); margin-bottom: 5px; letter-spacing: 0.04em;
}
.field input,
.field select,
.field textarea {
  width: 100%;
  padding: 12px 14px;
  border: 1.5px solid var(--mist);
  border-radius: var(--r-sm);
  font-family: 'DM Sans', sans-serif;
  font-size: 0.88rem;
  color: var(--stone);
  background: var(--linen);
  outline: none;
  transition: border-color 0.2s, background 0.2s;
  -webkit-appearance: none;
}
.field input:focus,
.field select:focus { border-color: var(--gold); background: var(--white); }
.field-row { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }

/* ── BUTTONS ── */
.btn {
  display: block; width: 100%;
  padding: 15px 20px;
  border-radius: var(--r-sm);
  border: none; cursor: pointer;
  font-family: 'DM Sans', sans-serif;
  font-size: 0.82rem; font-weight: 500;
  letter-spacing: 0.1em; text-transform: uppercase;
  text-align: center; text-decoration: none;
  transition: all 0.22s;
}
.btn-gold {
  background: var(--gold); color: var(--dark);
}
.btn-gold:hover { background: var(--gold-l); transform: translateY(-1px); box-shadow: 0 8px 24px rgba(184,144,42,0.3); }
.btn-gold:disabled { opacity: 0.4; pointer-events: none; transform: none; box-shadow: none; }
.btn-stone  { background: var(--stone); color: var(--gold-p); }
.btn-stone:hover { background: var(--bark); }
.btn-outline { background: transparent; color: var(--gold); border: 1.5px solid rgba(184,144,42,0.4); }
.btn-outline:hover { border-color: var(--gold); background: var(--gold-dim); }
.btn-ghost  { background: rgba(255,255,255,0.07); color: var(--clay); }
.btn-ghost:hover  { background: rgba(255,255,255,0.12); }
.btn-sm { padding: 10px 16px; font-size: 0.74rem; }

/* ── STICKY FOOTER CTA ── */
.sticky-footer {
  position: sticky; bottom: 0;
  padding: 14px 20px 28px;
  background: linear-gradient(to top, var(--linen) 75%, transparent);
  z-index: 50;
}
.sticky-footer.dark-footer {
  background: linear-gradient(to top, var(--dark) 75%, transparent);
}

/* ── CHIPS / TAGS ── */
.chip {
  display: inline-flex; align-items: center; gap: 4px;
  padding: 4px 10px; border-radius: 20px;
  font-size: 0.65rem; font-weight: 500; letter-spacing: 0.06em;
}
.chip-sage    { background: rgba(122,140,106,0.15); color: var(--ok); }
.chip-gold    { background: var(--gold-dim); color: var(--gold); }
.chip-mist    { background: var(--mist); color: var(--bark); }
.chip-promo   { background: var(--gold); color: var(--dark); font-weight: 600; }

/* ── BOTTOM NAV ── */
.bottom-nav {
  position: sticky; bottom: 0; z-index: 200;
  display: flex;
  background: rgba(253,248,240,0.96);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-top: 1px solid rgba(184,144,42,0.1);
  padding-bottom: env(safe-area-inset-bottom, 0px);
}
.bn-item {
  flex: 1; display: flex; flex-direction: column;
  align-items: center; gap: 3px; padding: 10px 0;
  font-size: 0.58rem; letter-spacing: 0.06em; text-transform: uppercase;
  color: rgba(58,52,40,0.35); cursor: pointer; text-decoration: none;
  transition: color 0.2s;
}
.bn-item.active { color: var(--gold); }
.bn-icon { font-size: 1.25rem; line-height: 1; }

/* ── DIVIDER ── */
.divider { height: 1px; background: var(--mist); margin: 14px 0; }

/* ── TOAST ── */
.toast {
  position: fixed; bottom: 90px; left: 50%; transform: translateX(-50%) translateY(20px);
  max-width: calc(var(--max-w) - 40px); width: calc(100% - 40px);
  background: var(--stone); color: var(--gold-p);
  padding: 12px 16px; border-radius: var(--r-sm);
  font-size: 0.82rem; border-left: 3px solid var(--gold);
  box-shadow: var(--shadow-lg); z-index: 999;
  opacity: 0; transition: all 0.35s cubic-bezier(0.22,1,0.36,1);
  pointer-events: none;
}
.toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }

/* ── ANIMATIONS ── */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(16px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes fadeIn {
  from { opacity: 0; }
  to   { opacity: 1; }
}
@keyframes pop {
  0%   { transform: scale(0.8); opacity: 0; }
  70%  { transform: scale(1.05); }
  100% { transform: scale(1); opacity: 1; }
}
.anim-fadeup { animation: fadeUp 0.5s cubic-bezier(0.22,1,0.36,1) both; }
.anim-pop    { animation: pop 0.5s cubic-bezier(0.22,1,0.36,1) both; }

/* stagger helpers */
.s1 { animation-delay: 0.05s; }
.s2 { animation-delay: 0.10s; }
.s3 { animation-delay: 0.15s; }
.s4 { animation-delay: 0.20s; }
.s5 { animation-delay: 0.25s; }

/* ── RESPONSIVE DESKTOP CENTERING ── */
@media (min-width: 520px) {
  body { background: #0e0c08; }
  .app-shell { margin: 0 auto; }
}
